# Binaries
testreg
/testreg
# Only the top-level `atlas` binary (`go build .`) — anchored so it does
# NOT match the `cmd/atlas/` package directory.
/atlas
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool
*.out
coverage.html

# Go workspace file
go.work
go.work.sum

# IDE
.idea/
.vscode/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Temporary files
*.tmp
*.bak

# Build output
/dist/
/bin/
.testreg-cache/
node_modules/
